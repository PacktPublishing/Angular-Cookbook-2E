import { ButtonFilledDirective } from './button-filled.directive';

describe('ButtonFilledDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonFilledDirective();
    expect(directive).toBeTruthy();
  });
});
