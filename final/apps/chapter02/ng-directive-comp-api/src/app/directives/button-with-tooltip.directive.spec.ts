import { ButtonWithTooltipDirective } from './button-with-tooltip.directive';

describe('ButtonWithTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonWithTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
