import { ButtonOutlinedDirective } from './button-outlined.directive';

describe('ButtonOutlinedDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonOutlinedDirective();
    expect(directive).toBeTruthy();
  });
});
