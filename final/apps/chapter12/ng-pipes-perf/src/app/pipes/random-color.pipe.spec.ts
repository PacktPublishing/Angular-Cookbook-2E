import { RandomColorPipe } from './random-color.pipe';

describe('RandomColorPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomColorPipe();
    expect(pipe).toBeTruthy();
  });
});
