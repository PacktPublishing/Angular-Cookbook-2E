import { MultTablePipe } from './mult-table.pipe';

describe('MultTablePipe', () => {
  it('create an instance', () => {
    const pipe = new MultTablePipe();
    expect(pipe).toBeTruthy();
  });
});
